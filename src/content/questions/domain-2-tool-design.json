[
  {
    "id": "d2-q01",
    "domain": 2,
    "taskStatement": "2.1",
    "difficulty": "foundation",
    "scenario": "A team defines 15 tools for a customer-service agent. When users ask to check order status, Claude sometimes calls update_order instead of get_order_status. Both accept order_id.",
    "question": "What is the most likely root cause of Claude selecting the wrong tool?",
    "options": {
      "A": "The tool descriptions are too similar and do not clearly differentiate read vs write semantics",
      "B": "The tools need to be reordered so get_order_status appears first in the array",
      "C": "The system prompt needs an explicit routing table mapping user intents to tool names",
      "D": "Claude cannot distinguish between tools with the same parameter names"
    },
    "correctAnswer": "A",
    "explanation": "Tool descriptions are the PRIMARY mechanism Claude uses for tool selection. When descriptions overlap semantically, Claude may pick the wrong one. Option B is wrong because array ordering does not determine selection priority. Option C is wrong because good descriptions eliminate the need for routing tables. Option D is wrong because Claude uses descriptions and names, not just parameter names.",
    "examTrap": "Option C is tempting because manual routing feels safe, but it fights against Claude natural tool selection.",
    "conceptsTested": [
      "tool descriptions as selection mechanism",
      "tool disambiguation"
    ]
  },
  {
    "id": "d2-q02",
    "domain": 2,
    "taskStatement": "2.1",
    "difficulty": "intermediate",
    "scenario": "An e-commerce agent has search_products and get_product_details. Claude calls get_product_details when users search by price. search_products says Searches the catalog and get_product_details says Gets details about products.",
    "question": "What is the best way to fix the tool selection issue?",
    "options": {
      "A": "Add parameter validation to reject calls without a product ID",
      "B": "Rewrite descriptions to be action-specific: search_products should say Searches the catalog by keyword, category, or price range returning a list of matching products while get_product_details says Retrieves full specifications for a single product given its unique product_id",
      "C": "Use tool_choice to force search_products for price-related messages",
      "D": "Merge get_product_details into search_products"
    },
    "correctAnswer": "B",
    "explanation": "Precise action-specific descriptions that explain WHEN to use each tool and WHAT it returns are the best fix. Option A treats the symptom instead of the cause. Option C removes Claude ability to reason about selection. Option D eliminates useful functionality.",
    "examTrap": "Option A seems pragmatic but wastes an API round-trip and does not prevent future misselection.",
    "conceptsTested": [
      "tool description specificity",
      "action-specific descriptions"
    ]
  },
  {
    "id": "d2-q03",
    "domain": 2,
    "taskStatement": "2.1",
    "difficulty": "advanced",
    "scenario": "A financial services platform provides Claude with 40+ tools spanning account management, trading, compliance, and reporting. Despite detailed descriptions, Claude occasionally selects low-relevance tools, increasing latency.",
    "question": "What is the most effective approach to improve tool selection at this scale?",
    "options": {
      "A": "Implement dynamic tool filtering that provides only 8-10 most relevant tools per turn based on context",
      "B": "Add numbered priority hints to each tool description",
      "C": "Group all tools into a single mega-tool with a sub_action parameter",
      "D": "Increase model temperature to explore more tool options"
    },
    "correctAnswer": "A",
    "explanation": "Dynamic tool filtering reduces the selection space, improving accuracy and latency. Option B is wrong because Claude does not interpret numeric priority metadata. Option C defeats typed tool definitions. Option D increases randomness, worsening selection.",
    "examTrap": "Option C seems to simplify things but moves complexity into the parameter schema and loses type safety.",
    "conceptsTested": [
      "dynamic tool filtering",
      "tool set scaling",
      "context-aware tool presentation"
    ]
  },
  {
    "id": "d2-q04",
    "domain": 2,
    "taskStatement": "2.1",
    "difficulty": "foundation",
    "scenario": "A developer creates a tool called process_data with description Processes data. During testing, Claude rarely selects it even when appropriate, preferring text responses. Other tools with longer descriptions work correctly.",
    "question": "Why does Claude avoid selecting this tool?",
    "options": {
      "A": "The tool name process_data is too generic for Claude",
      "B": "The description is too vague for Claude to determine when the tool is appropriate since Claude relies on descriptions to understand tool purpose and applicability",
      "C": "Claude has a minimum description length requirement enforced by the API",
      "D": "Single-word descriptions cause deprioritization by the API ranking algorithm"
    },
    "correctAnswer": "B",
    "explanation": "Claude uses tool descriptions as the primary signal for deciding when to invoke a tool. Processes data provides no information about WHAT kind of data, WHEN to use it, or WHAT it returns. Option A is wrong because names help but descriptions are primary. Option C is wrong because the API does not enforce minimum lengths. Option D is wrong because there is no ranking algorithm.",
    "examTrap": "Option A is tempting because the name IS generic, but a specific description would fix the problem.",
    "conceptsTested": [
      "tool description quality",
      "description as selection signal"
    ]
  },
  {
    "id": "d2-q05",
    "domain": 2,
    "taskStatement": "2.2",
    "difficulty": "foundation",
    "scenario": "A developer builds an agentic system where tools throw JavaScript exceptions on errors. The agent loop catches exceptions and retries the conversation turn. In production, the agent gets stuck retrying non-transient errors like User not found.",
    "question": "What is the correct pattern for handling tool errors?",
    "options": {
      "A": "Wrap all tool calls in try-catch and return a generic Tool failed message",
      "B": "Return structured error responses via tool_result with is_error: true, including error type and actionable details so Claude can reason about the failure",
      "C": "Log errors externally and let Claude continue without the tool result",
      "D": "Implement automatic retries with exponential backoff for all errors"
    },
    "correctAnswer": "B",
    "explanation": "Structured error responses using is_error: true let Claude see what went wrong and decide intelligently. Option A loses error context. Option C violates the protocol since Claude expects a tool_result for every tool_use. Option D retries non-transient errors wastefully.",
    "examTrap": "Option D seems robust but treats all errors as transient. User not found will never succeed on retry.",
    "conceptsTested": [
      "structured error responses",
      "is_error flag",
      "tool_result protocol"
    ]
  },
  {
    "id": "d2-q06",
    "domain": 2,
    "taskStatement": "2.2",
    "difficulty": "intermediate",
    "scenario": "A team implements is_error: true for structured errors. However, Claude often apologizes and stops instead of retrying when it receives transient errors like Connection timeout.",
    "question": "How should the team improve their error response strategy?",
    "options": {
      "A": "Remove is_error for transient errors and return them as successful results",
      "B": "Include error categorization (transient vs permanent) and suggested recovery actions in the error response",
      "C": "Handle all retries in the application layer before returning to Claude",
      "D": "Add a system prompt telling Claude to always retry after any error"
    },
    "correctAnswer": "B",
    "explanation": "Error categories and recovery hints give Claude context to decide intelligently. Option A obscures error semantics. Option C removes Claude agency to decide recovery. Option D causes retries even for permanent errors.",
    "examTrap": "Option C seems clean but removes Claude ability to reason about errors and adapt strategy.",
    "conceptsTested": [
      "error categorization",
      "transient vs permanent errors",
      "recovery hints"
    ]
  },
  {
    "id": "d2-q07",
    "domain": 2,
    "taskStatement": "2.2",
    "difficulty": "advanced",
    "scenario": "A multi-tool agent orchestrates database queries, API calls, and file ops. When a tool fails mid-workflow (3rd of 5 ops), Claude abandons the entire workflow instead of handling partial completion.",
    "question": "What is the best pattern for handling partial workflow failures?",
    "options": {
      "A": "Implement two-phase commit across all tools for atomicity",
      "B": "Return structured errors that include both error details AND partial results achieved, enabling Claude to decide whether to continue, retry, or roll back",
      "C": "Queue all operations and execute as a single batch transaction",
      "D": "Catch errors silently and return synthetic success responses"
    },
    "correctAnswer": "B",
    "explanation": "Returning partial results with the error lets Claude make informed decisions. Option A is impractical for heterogeneous tool types. Option C assumes all tools support batching. Option D masks failures risking corruption.",
    "examTrap": "Option A sounds correct but two-phase commit across DB, REST, and filesystem is extremely complex and often impossible.",
    "conceptsTested": [
      "partial failure handling",
      "structured error with partial results",
      "workflow resilience"
    ]
  },
  {
    "id": "d2-q08",
    "domain": 2,
    "taskStatement": "2.2",
    "difficulty": "intermediate",
    "scenario": "A tool returns errors as plain text in tool_result: ERROR: Invalid date format. Expected YYYY-MM-DD. Claude receives this as a normal result. The developer wonders about using is_error: true instead.",
    "question": "What is the practical difference between error text in normal tool_result vs is_error: true?",
    "options": {
      "A": "No practical difference since Claude processes both identically",
      "B": "is_error: true causes the API to automatically retry the tool call",
      "C": "is_error: true signals to Claude that the tool execution failed, influencing reasoning to treat the result as an error condition and triggering different response strategies",
      "D": "is_error: true prevents Claude from including error details in its response"
    },
    "correctAnswer": "C",
    "explanation": "The is_error flag is a semantic signal telling Claude the call failed, influencing its reasoning. Option A is wrong because the flag does affect behavior. Option B is wrong as there is no automatic retry. Option D is wrong as Claude can still share error details.",
    "examTrap": "Option A is tempting because Claude CAN figure out something went wrong from error text, but is_error provides an unambiguous signal.",
    "conceptsTested": [
      "is_error semantics",
      "error signaling",
      "tool_result error handling"
    ]
  },
  {
    "id": "d2-q09",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "foundation",
    "scenario": "A developer builds a chatbot that can optionally search the web. For most messages, Claude should respond from training data. The developer sets tool_choice to any to ensure tools are available.",
    "question": "Why is tool_choice any problematic here?",
    "options": {
      "A": "The any mode is deprecated and will be removed",
      "B": "any forces Claude to use a tool on EVERY turn, even when text would be appropriate, preventing text-only responses",
      "C": "any only works with a single tool definition",
      "D": "any causes Claude to call all available tools simultaneously"
    },
    "correctAnswer": "B",
    "explanation": "tool_choice any means Claude MUST call at least one tool. For a chatbot where most responses should come from Claude knowledge, this forces unnecessary tool calls. The correct mode is auto. Option A is wrong. Option C is wrong. Option D is wrong.",
    "examTrap": "Developers confuse any (must use a tool) with auto (may use a tool). The name any sounds permissive but is a constraint.",
    "conceptsTested": [
      "tool_choice modes",
      "any vs auto",
      "forced tool use"
    ]
  },
  {
    "id": "d2-q10",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "intermediate",
    "scenario": "A form-filling app needs Claude to always return structured output via a tool call. The developer wants to force a specific tool.",
    "question": "What is the correct tool_choice configuration to force a specific tool?",
    "options": {
      "A": "tool_choice: { type: auto, tool_name: extract_fields }",
      "B": "tool_choice: { type: tool, name: extract_fields }",
      "C": "tool_choice: { type: any, preferred: extract_fields }",
      "D": "tool_choice: { type: required, tool: extract_fields }"
    },
    "correctAnswer": "B",
    "explanation": "type: tool with name forces Claude to call that specific tool every turn. Option A mixes auto with a tool name which is invalid. Option C uses non-existent preferred parameter. Option D uses required which is not a valid type.",
    "examTrap": "Option A looks reasonable but auto mode means Claude chooses freely.",
    "conceptsTested": [
      "tool_choice specific tool",
      "forced tool calling",
      "API syntax"
    ]
  },
  {
    "id": "d2-q11",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "advanced",
    "scenario": "A multi-step insurance claims agent requires Step 1: validate_claim, Step 2: assess_risk, Step 3: generate_decision in exact sequence.",
    "question": "What is the best approach to enforce tool sequencing?",
    "options": {
      "A": "Use tool_choice with the specific tool name for each turn, updating programmatically as each step completes",
      "B": "Define all steps as parameters of a single workflow tool",
      "C": "Use tool_choice any and add sequencing instructions to the system prompt",
      "D": "Create a separate conversation for each step"
    },
    "correctAnswer": "A",
    "explanation": "Dynamically setting tool_choice per step gives deterministic control from the application layer. Option B collapses the workflow losing inter-step reasoning. Option C relies on unreliable prompt instructions. Option D wastes context.",
    "examTrap": "Option C seems reasonable but sequencing is a hard constraint that should be enforced programmatically, not probabilistically.",
    "conceptsTested": [
      "dynamic tool_choice",
      "workflow sequencing",
      "programmatic tool control"
    ]
  },
  {
    "id": "d2-q12",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "foundation",
    "scenario": "A developer uses tool_choice auto for a coding assistant. Claude sometimes gives text explanations instead of running code via execute_code tool.",
    "question": "What does tool_choice auto actually mean?",
    "options": {
      "A": "Claude automatically calls the most relevant tool for every message",
      "B": "Claude decides whether to use a tool based on its judgment, it may respond with text, one tool, or multiple tools",
      "C": "Claude alternates between text and tool calls",
      "D": "Claude only uses tools if the user explicitly requests it"
    },
    "correctAnswer": "B",
    "explanation": "auto gives Claude full discretion. It analyzes the message and context to determine the best response. Option A describes any mode. Option C implies a non-existent pattern. Option D is wrong because Claude proactively uses tools.",
    "examTrap": "The word auto misleads developers into thinking it means automatically use tools. It means Claude decides, including not using tools.",
    "conceptsTested": [
      "tool_choice auto semantics",
      "Claude tool discretion"
    ]
  },
  {
    "id": "d2-q13",
    "domain": 2,
    "taskStatement": "2.4",
    "difficulty": "foundation",
    "scenario": "A team needs an AI assistant with PostgreSQL, Slack, and GitHub access. A developer proposes building custom tool implementations from scratch.",
    "question": "According to MCP best practices, what should the team do FIRST?",
    "options": {
      "A": "Build custom tools first, migrate to MCP later",
      "B": "Check for existing community MCP servers for PostgreSQL, Slack, and GitHub, evaluating community servers before building custom",
      "C": "Write a unified MCP server for all three",
      "D": "Use Claude built-in knowledge instead of tool integrations"
    },
    "correctAnswer": "B",
    "explanation": "MCP best practice is community servers first. Pre-built servers exist for databases, Slack, and GitHub. Option A wastes effort. Option C is overly complex. Option D cannot replace real-time data access.",
    "examTrap": "Option A feels pragmatic but ignores the MCP ecosystem primary value: reusable community integrations.",
    "conceptsTested": [
      "MCP community servers first",
      "MCP best practices",
      "integration strategy"
    ]
  },
  {
    "id": "d2-q14",
    "domain": 2,
    "taskStatement": "2.4",
    "difficulty": "intermediate",
    "scenario": "A developer learning MCP has a knowledge base Claude should reference, a deployment pipeline Claude should trigger, and standard prompt templates for the team.",
    "question": "How should these map to MCP primitives?",
    "options": {
      "A": "Knowledge base to resources, deployment pipeline to tools, prompt templates to prompts",
      "B": "Knowledge base to tools, deployment pipeline to prompts, templates to resources",
      "C": "Knowledge base to prompts, deployment pipeline to tools, templates to resources",
      "D": "All three should be tools since tools are most flexible"
    },
    "correctAnswer": "A",
    "explanation": "Resources are data the model reads. Tools are actions the model executes. Prompts are reusable templates. Option B confuses all three. Option C swaps resources and prompts. Option D ignores the purpose of distinct primitives.",
    "examTrap": "Option D is tempting because tools CAN do everything, but using correct primitive types improves discoverability.",
    "conceptsTested": [
      "MCP primitives",
      "resources vs tools vs prompts",
      "MCP architecture"
    ]
  },
  {
    "id": "d2-q15",
    "domain": 2,
    "taskStatement": "2.4",
    "difficulty": "advanced",
    "scenario": "A company has MCP servers for GitHub, PostgreSQL, and Jira. They need to add their proprietary billing system with complex auth, rate limiting, and compliance error handling.",
    "question": "What is the correct approach for adding this proprietary integration?",
    "options": {
      "A": "Bypass MCP and connect directly as Claude tool definitions",
      "B": "Build a custom MCP server that implements the MCP protocol, handling proprietary auth, rate limiting, and compliance within the server",
      "C": "Fork an existing community MCP server and modify it",
      "D": "Use an MCP gateway to auto-translate the billing API"
    },
    "correctAnswer": "B",
    "explanation": "When no community server exists, build a custom MCP server. It encapsulates proprietary logic while exposing clean MCP interfaces. Option A breaks the MCP pattern. Option C starts from irrelevant code. Option D describes a non-existent product.",
    "examTrap": "Option A seems simpler but fragments the architecture: some integrations use MCP, others do not.",
    "conceptsTested": [
      "custom MCP server",
      "MCP architecture consistency",
      "proprietary integrations"
    ]
  },
  {
    "id": "d2-q16",
    "domain": 2,
    "taskStatement": "2.4",
    "difficulty": "intermediate",
    "scenario": "An MCP server exposes resources (customer profiles, order history) and tools (create_order, update_customer) for a CRM. A developer asks why not make everything a tool.",
    "question": "What is the key reason for using MCP resources for read-only data?",
    "options": {
      "A": "Resources are faster because they skip model reasoning",
      "B": "Resources can be attached to context by the application or user without requiring the model to invoke them, while tools are actions the model actively decides to call",
      "C": "Resources are cached by MCP protocol while tools are not",
      "D": "Resources have a smaller token footprint"
    },
    "correctAnswer": "B",
    "explanation": "The fundamental distinction is control: resources are application/user-controlled while tools are model-controlled. Option A is wrong as both use MCP protocol. Option C is wrong as caching is an implementation detail. Option D is wrong as token size depends on content.",
    "examTrap": "Option C sounds plausible but the resource/tool distinction is about control semantics, not performance.",
    "conceptsTested": [
      "resources vs tools",
      "MCP control model",
      "application vs model controlled"
    ]
  },
  {
    "id": "d2-q17",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "foundation",
    "scenario": "A developer building a code assistant starts implementing custom read_file, write_file, list_directory tools. A colleague suggests checking Claude built-in capabilities first.",
    "question": "Why prefer built-in tools over custom implementations for file operations?",
    "options": {
      "A": "Built-in tools are free and do not count against token usage",
      "B": "Built-in tools like those in Claude Code are already optimized, tested, and include safety features like permission checks, making reimplementation redundant and error-prone",
      "C": "Custom tools are not allowed for filesystem operations",
      "D": "Built-in tools run faster by bypassing network round-trips"
    },
    "correctAnswer": "B",
    "explanation": "Built-in tools are production-tested with security guardrails and edge case handling. Option A is wrong as built-in tools consume tokens. Option C is wrong as custom file tools are allowed. Option D is wrong as built-in tools use the same protocol.",
    "examTrap": "Option D sounds plausible because built-in implies local, but Claude built-in tools still use the standard tool-use protocol.",
    "conceptsTested": [
      "built-in vs custom tools",
      "tool reuse",
      "avoiding redundant implementations"
    ]
  },
  {
    "id": "d2-q18",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "intermediate",
    "scenario": "A team uses Claude Code but needs to interact with a custom internal API with proprietary authentication and non-standard REST conventions.",
    "question": "Should they use a built-in tool or create a custom tool?",
    "options": {
      "A": "Use built-in web request tools with auth headers as parameters",
      "B": "Create a custom tool because the internal API has proprietary auth and non-standard conventions that built-in tools cannot accommodate",
      "C": "Modify built-in tool configuration to support custom auth",
      "D": "Use built-in tools with a proxy server adding auth headers"
    },
    "correctAnswer": "B",
    "explanation": "Custom tools are appropriate when integration requires proprietary logic built-in tools cannot handle. Option A assumes built-in tools handle arbitrary auth. Option C assumes built-in config is modifiable. Option D adds unnecessary infrastructure.",
    "examTrap": "Option D is clever but adds latency and maintenance burden for something a custom tool handles cleanly.",
    "conceptsTested": [
      "when to use custom tools",
      "proprietary integrations",
      "built-in tool limitations"
    ]
  },
  {
    "id": "d2-q19",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "advanced",
    "scenario": "A platform team designs an agent for 50 developers needing file ops, git, code search, and terminal access. They debate custom vs built-in tools, with some worrying about built-in tools being too permissive.",
    "question": "What is the optimal strategy for balancing built-in and custom tools?",
    "options": {
      "A": "Replace all built-in tools with custom implementations enforcing security policies",
      "B": "Use built-in tools for standard operations and layer custom permission checks and audit logging via configuration, adding custom tools only for domain-specific capabilities",
      "C": "Disable all built-in tools and provide only custom tools with explicit allowlists",
      "D": "Use built-in tools without modification, enforcing security at infrastructure level only"
    },
    "correctAnswer": "B",
    "explanation": "Use built-in tools as foundation (tested, maintained) while adding governance layers. Custom tools for domain-specific needs only. Option A wastes effort reimplementing. Option C creates massive maintenance. Option D provides no application-level security.",
    "examTrap": "Option A feels secure but reimplementing 4+ complex tool categories introduces more bugs than it prevents.",
    "conceptsTested": [
      "built-in tool governance",
      "security layering",
      "tool strategy at scale"
    ]
  },
  {
    "id": "d2-q20",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "foundation",
    "scenario": "A developer considers creating a custom run_tests tool. Claude Code already has a bash/terminal tool that can execute any shell command including npm test and pytest.",
    "question": "What is the strongest argument for the built-in terminal tool?",
    "options": {
      "A": "Custom tools require API approval from Anthropic",
      "B": "The built-in terminal tool already handles command execution, output streaming, and error codes, making a custom run_tests tool a thin wrapper adding no capability",
      "C": "Built-in tools are always faster than custom tools",
      "D": "Claude cannot use custom tools as effectively as built-in ones"
    },
    "correctAnswer": "B",
    "explanation": "When a built-in tool covers the use case, a custom tool adds indirection without capability. Option A is wrong as custom tools need no approval. Option C is wrong as speed depends on implementation. Option D is wrong as Claude uses custom tools effectively.",
    "examTrap": "Some developers create dedicated tools for every task, but if the built-in handles it, the custom tool just adds overhead.",
    "conceptsTested": [
      "avoiding unnecessary custom tools",
      "built-in tool sufficiency",
      "tool design judgment"
    ]
  },
  {
    "id": "d2-q21",
    "domain": 2,
    "taskStatement": "2.2",
    "difficulty": "intermediate",
    "scenario": "A travel booking agent uses a search_flights tool that queries an airline API. A user asks to find direct flights from SFO to JFK on December 25th. The API responds successfully with HTTP 200 but returns zero matching flights because no airline operates that specific route on that date. The developer is deciding how the tool result should be structured. Currently the tool throws a NoResultsError when zero flights are found, which Claude interprets as a tool failure and tells the user 'I encountered an error searching for flights.' The user is confused because the search worked — there simply were no flights available on that date.",
    "question": "How should the search_flights tool handle a successful query that returns zero results?",
    "options": {
      "A": "Return a structured result like { results: [], message: 'No direct flights found for this route and date' } so Claude can relay the empty outcome to the user as a valid finding",
      "B": "Throw a NoResultsError to signal to Claude that no data was found, allowing the error handler to generate a user-friendly message",
      "C": "Return null so Claude knows the search produced nothing and can generate its own explanation of why no flights exist",
      "D": "Retry the search automatically with relaxed parameters such as adding connecting flights, since empty results indicate the query was too restrictive"
    },
    "correctAnswer": "A",
    "explanation": "An empty result set from a successful query is NOT an error — it is valid information meaning 'we searched and found nothing.' Returning a structured empty result lets Claude accurately communicate the outcome. Option B conflates 'no matches' with 'search failed,' causing Claude to report errors when none occurred. Option C loses context about what was searched. Option D silently changes the user's request without consent, and the tool should not make autonomous decisions about query parameters.",
    "examTrap": "Option B feels like standard error handling, but throwing errors for empty results is a common anti-pattern that confuses Claude about whether the tool actually worked.",
    "conceptsTested": [
      "empty results vs errors",
      "tool result design",
      "successful empty responses"
    ]
  },
  {
    "id": "d2-q22",
    "domain": 2,
    "taskStatement": "2.2",
    "difficulty": "advanced",
    "scenario": "A customer support agent has a lookup_account tool that queries a PostgreSQL database. During a conversation, the agent calls lookup_account with email 'jane@example.com'. The database connection times out after 30 seconds because the database server is under heavy load. In a separate conversation five minutes later, another user asks the agent to look up 'nonexistent@fake.com' — the query executes successfully in 50ms but returns zero rows because no account exists with that email. The development team is debating whether both situations should return the same error response structure, since in both cases the user does not receive account data.",
    "question": "Which statement correctly distinguishes these two scenarios for tool result design?",
    "options": {
      "A": "Both should return an error because in both cases the tool failed to provide the requested account information to Claude",
      "B": "The connection timeout is an error because the tool could not execute the query, while zero rows is a valid empty result because the query executed successfully — only the timeout should use is_error: true",
      "C": "The zero-rows case should return an error because a missing account means the user provided invalid input, while the timeout should silently retry without informing Claude",
      "D": "Neither should be treated as an error — the timeout should return an empty result with a retry suggestion, and the zero-rows case should return null"
    },
    "correctAnswer": "B",
    "explanation": "The critical distinction is whether the tool successfully executed its operation. A connection timeout means the tool COULD NOT perform the search — that is a genuine error deserving is_error: true. Zero rows means the tool DID perform the search and found nothing — that is a valid result. Option A incorrectly treats successful queries as errors. Option C blames the user for data that does not exist and hides infrastructure problems. Option D masks real infrastructure failures by presenting them as empty results, which prevents Claude from communicating the actual problem to the user.",
    "examTrap": "Option A seems reasonable because neither case provides account data, but the distinction between 'could not search' and 'searched and found nothing' is fundamental to tool result design.",
    "conceptsTested": [
      "error vs empty result distinction",
      "is_error flag usage",
      "infrastructure errors vs data absence"
    ]
  },
  {
    "id": "d2-q23",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "foundation",
    "scenario": "A developer joins a new Python monorepo with hundreds of files spread across multiple directories including src/, tests/, scripts/, and docs/. Before making changes, they want to understand the project structure by finding all Python files (files ending in .py) across the entire repository. They open Claude Code and consider which built-in tool to use. The repository has .py files, .pyc files, .pyi stub files, and various configuration files like setup.cfg and pyproject.toml. The developer specifically wants to locate source files by their filename extension, not search inside file contents.",
    "question": "Which Claude Code built-in tool should the developer use to find all .py files?",
    "options": {
      "A": "Glob — it matches files by name patterns like **/*.py, which is exactly designed for finding files by extension across directories",
      "B": "Grep — it can search for the .py extension string across the filesystem to locate Python files",
      "C": "Read — it can scan directory listings recursively to find files matching a pattern",
      "D": "Bash with find command — built-in tools cannot handle recursive file pattern matching"
    },
    "correctAnswer": "A",
    "explanation": "Glob is purpose-built for matching files by name patterns. The pattern **/*.py efficiently finds all Python files across all subdirectories. Option B is wrong because Grep searches file CONTENTS, not file names — it would find files that contain the text '.py' inside them, not files named with the .py extension. Option C is wrong because Read retrieves file contents, not file listings. Option D is wrong because Glob handles this natively without needing shell commands.",
    "examTrap": "Option B is the classic trap — Grep sounds like it could 'search for .py files' but it searches file contents, not file names. This Glob vs Grep distinction is a key exam concept.",
    "conceptsTested": [
      "Glob vs Grep distinction",
      "file name pattern matching",
      "built-in tool selection"
    ]
  },
  {
    "id": "d2-q24",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "intermediate",
    "scenario": "A security engineer is auditing a large Python codebase to find every file that imports the anthropic SDK. The codebase has 400+ Python files across multiple packages. The engineer needs to find all files that contain the line 'import anthropic' or 'from anthropic import' anywhere in their source code. They know the files are all .py files, but they need to search the actual content of each file to determine which ones use the Anthropic SDK. The engineer opens Claude Code and considers which built-in tool will most efficiently accomplish this content-based search across hundreds of files.",
    "question": "Which Claude Code built-in tool should the engineer use to find files containing 'import anthropic'?",
    "options": {
      "A": "Glob with pattern **/*anthropic* — this will match any file with 'anthropic' in its path or name",
      "B": "Grep with pattern 'import anthropic' scoped to **/*.py files — it searches file contents for matching text patterns",
      "C": "Read each .py file individually and check if it contains the import statement",
      "D": "Bash with a custom Python script that opens every file and searches for the string"
    },
    "correctAnswer": "B",
    "explanation": "Grep is designed to search file CONTENTS for text patterns. It efficiently scans all .py files for 'import anthropic' and returns matching files and lines. Option A is wrong because Glob matches file NAMES, not contents — **/*anthropic* would only find files with 'anthropic' in their filename, not files that import the module. Option C would work but is extremely inefficient, requiring hundreds of individual Read calls. Option D adds unnecessary complexity when a built-in tool handles this directly.",
    "examTrap": "Option A is tempting because Glob seems like it could 'search for anthropic,' but Glob only matches filenames and paths — it cannot look inside files at all.",
    "conceptsTested": [
      "Grep for content search",
      "Glob vs Grep distinction",
      "content-based file discovery"
    ]
  },
  {
    "id": "d2-q25",
    "domain": 2,
    "taskStatement": "2.5",
    "difficulty": "advanced",
    "scenario": "A Claude Code agent is modifying a large configuration file (2,500 lines) that contains complex nested YAML structures. The agent uses the Edit tool to replace a 15-line block defining a database connection pool. The Edit tool fails with an error indicating it cannot find the exact match for the old_str parameter because the file was recently reformatted and whitespace changed between the agent reading the file and attempting the edit. The agent needs to complete the modification to proceed with the deployment pipeline. The developer managing this agent wants to define a fallback strategy in the CLAUDE.md to handle Edit tool failures gracefully without requiring human intervention.",
    "question": "What is the recommended fallback when the Edit tool fails on a file modification?",
    "options": {
      "A": "Retry the Edit tool with a larger context window in old_str to increase the chance of a unique match",
      "B": "Fall back to a Read → modify in memory → Write sequence — read the current file contents, apply the change programmatically, and write the entire file back",
      "C": "Use Bash to run sed or awk to perform the text replacement since shell tools handle whitespace more flexibly",
      "D": "Skip the modification and log a warning for the human operator to handle manually"
    },
    "correctAnswer": "B",
    "explanation": "The Read → modify → Write pattern is the documented reliability fallback for Edit failures. Reading the current file ensures you have the actual content (including any whitespace changes), modifying in memory gives full control, and Write replaces the file atomically. Option A may work for minor issues but does not address fundamental content drift. Option C introduces fragile shell text processing on complex YAML and risks corruption. Option D breaks automation and defeats the purpose of an autonomous agent.",
    "examTrap": "Option A seems like the logical first step, but when the file has changed underneath you, retrying Edit without re-reading the file will keep failing. The Read→Write pattern is the curriculum's taught reliability pattern.",
    "conceptsTested": [
      "Edit tool fallback pattern",
      "Read-modify-Write reliability",
      "tool failure recovery"
    ]
  },
  {
    "id": "d2-q26",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "foundation",
    "scenario": "A developer is building a knowledge-base assistant where the first user message always requires looking up relevant documentation before generating a response. The developer wants to guarantee that Claude calls the lookup_docs tool on the very first turn rather than attempting to answer from its training data. In subsequent turns, Claude should be free to decide whether another lookup is needed or whether it can respond directly. The developer is configuring the tool_choice parameter in the API request and reviewing the available options: auto, any, none, and tool (with a specific name).",
    "question": "Which tool_choice configuration forces Claude to call the lookup_docs tool on the first turn?",
    "options": {
      "A": "tool_choice: { type: 'auto' } — Claude will automatically choose lookup_docs when it is relevant",
      "B": "tool_choice: { type: 'any' } — Claude must use some tool, and since lookup_docs is most relevant it will choose it",
      "C": "tool_choice: { type: 'tool', name: 'lookup_docs' } — this forces Claude to call exactly the lookup_docs tool",
      "D": "tool_choice: { type: 'none' } with a system prompt instruction saying 'Always call lookup_docs first'"
    },
    "correctAnswer": "C",
    "explanation": "Setting tool_choice to { type: 'tool', name: 'lookup_docs' } forces Claude to invoke that specific tool. This is the only option that guarantees lookup_docs is called. Option A lets Claude decide, and it may skip the tool if it thinks it can answer directly. Option B forces a tool call but does not guarantee WHICH tool is selected. Option D disables tool use entirely, making it impossible for Claude to call any tool regardless of system prompt instructions.",
    "examTrap": "Option B is tempting because it forces tool use, but 'any' only guarantees SOME tool is called — if multiple tools exist, Claude might pick the wrong one.",
    "conceptsTested": [
      "tool_choice type: tool",
      "forcing specific tool invocation",
      "tool_choice modes"
    ]
  },
  {
    "id": "d2-q27",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "intermediate",
    "scenario": "A developer is building a multi-tool research assistant with five tools: search_web, search_academic, search_news, summarize_document, and extract_citations. The developer wants to ensure that Claude always uses at least one tool to gather information before responding to the user's research question, but the developer does not want to prescribe which specific tool Claude should use — Claude should reason about which tool is most appropriate based on the query. For example, a question about recent events should trigger search_news, while a question about published studies should trigger search_academic. The developer needs the right tool_choice setting to enforce tool use without restricting tool selection.",
    "question": "Which tool_choice configuration ensures Claude calls some tool without specifying which one?",
    "options": {
      "A": "tool_choice: { type: 'auto' } — Claude will decide to use a tool because the system prompt says to",
      "B": "tool_choice: { type: 'any' } — forces Claude to use at least one tool while letting it choose which one based on the query",
      "C": "tool_choice: { type: 'none' } — disables tool selection so Claude focuses on reasoning first before tools are re-enabled",
      "D": "tool_choice: { type: 'tool', name: 'search_web' } — defaulting to web search covers the most common case"
    },
    "correctAnswer": "B",
    "explanation": "tool_choice: { type: 'any' } guarantees Claude will invoke a tool but leaves the selection to Claude's judgment. This is ideal when you want mandatory tool use with flexible selection. Option A does not guarantee tool use — Claude may skip tools and answer from training data even with system prompt instructions. Option C disables all tools. Option D forces a specific tool, removing Claude's ability to choose the most appropriate one for each query type.",
    "examTrap": "Option A seems safe because a system prompt instruction should work, but auto means Claude CAN skip tools — there is no enforcement, only a suggestion.",
    "conceptsTested": [
      "tool_choice type: any",
      "mandatory tool use",
      "flexible tool selection"
    ]
  },
  {
    "id": "d2-q28",
    "domain": 2,
    "taskStatement": "2.3",
    "difficulty": "advanced",
    "scenario": "A developer builds a multi-turn customer service agent with tools for looking up orders, checking inventory, and processing returns. To ensure thorough responses, the developer sets tool_choice: { type: 'any' } on EVERY turn of the conversation. During testing, the agent successfully looks up orders and checks inventory, but it never produces a final text response to the customer. After calling process_return, the agent calls check_inventory again unnecessarily, then calls lookup_order again, looping through tools without ever delivering a summary to the user. The conversation grows indefinitely with repeated tool calls and no resolution.",
    "question": "What is the root cause of the agent's inability to deliver a final response?",
    "options": {
      "A": "The tools are not returning a 'done' flag to signal completion to Claude",
      "B": "Using tool_choice: { type: 'any' } on every turn forces Claude to always call a tool, preventing it from ever producing a text-only response to the user — it should be used only on the first turn and then switched to 'auto'",
      "C": "The system prompt needs an explicit instruction telling Claude when to stop using tools and respond with text",
      "D": "The agent needs a maximum turn limit to force termination after a set number of tool calls"
    },
    "correctAnswer": "B",
    "explanation": "tool_choice: { type: 'any' } forces Claude to call a tool on every turn, which means it can NEVER produce a text-only response. The correct pattern is to use 'any' on the first turn to force initial data gathering, then switch to 'auto' for subsequent turns so Claude can choose between making additional tool calls or delivering a final text response. Option A adds unnecessary protocol complexity. Option C cannot override tool_choice enforcement — system prompts are weaker than API parameters. Option D is a band-aid that does not address the root cause and still produces no final response.",
    "examTrap": "Option C sounds reasonable because system prompts often guide behavior, but tool_choice: 'any' is an API-level enforcement that system prompt instructions cannot override.",
    "conceptsTested": [
      "tool_choice anti-patterns",
      "any vs auto switching",
      "multi-turn tool_choice strategy"
    ]
  }
]